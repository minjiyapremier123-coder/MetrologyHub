version: '3.8'
services:
  backend:
    build: ./backend
    ports:
      - "5001:5001"
      - CHOKIDAR_USEPOLLING=true
    volumes:
      - ./backend/uploads:/usr/src/app/uploads
  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
